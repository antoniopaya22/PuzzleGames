<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="180"
        android:entries="@array/pref_sync_frec_tiempo"
        android:entryValues="@array/pref_sync_frec_val"
        android:key="sync_frec"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_sync_fec" />

    <Preference android:title="@string/pref_sync_ajustes">
        <intent android:action="android.settings.SYNC_SETTINGS" />
    </Preference>

</PreferenceScreen>
